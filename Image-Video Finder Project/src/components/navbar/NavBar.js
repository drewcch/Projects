import React, { Component } from "react";
import AppBar from "material-ui/AppBar";

const NavBar = () => (
  <AppBar
    title="PixaBay Image and Video Finder"
    style={{
      background: "linear-gradient(to right, #1a2a6c, #b21f1f, #fdbb2d)"
    }}
  />
);

export default NavBar;
